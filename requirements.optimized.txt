# Core application dependencies - optimized for size
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
requests>=2.31.0
jsonschema>=4.17.0
kubernetes>=27.2.0
psutil>=5.9.0
ollama>=0.1.7
aiohttp>=3.8.0
python-dateutil>=2.8.0

# Removed for size optimization:
# - torch (1GB+) - not essential for kubectl operations
# - transformers (500MB+) - using Ollama for LLM
# - scikit-learn (200MB+) - not used in current implementation
# - matplotlib/seaborn/plotly - UI uses streamlit native charts
# - openai - using Ollama instead
# - asyncio-mqtt/redis/SQLAlchemy/fastapi/uvicorn/websockets - not needed for current features
